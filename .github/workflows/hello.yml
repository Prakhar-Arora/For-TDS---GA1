name: Hello World Workflow

on:
  push:
    branches:
      - main  # or your default branch name

jobs:
  test:
    steps:
      - name: 22f3000671@ds.study.iitm.ac.in
        run: echo "Hello, world!"
